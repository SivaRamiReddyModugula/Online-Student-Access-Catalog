spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:../configs/
  profiles:
    active:
      - native
server:
  port: 8888
